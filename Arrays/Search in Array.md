## Search Operation in Array

we use a function called index() which returns the index of the element passed into it as an argument 

```python 

from array import*
array_name = array('i',[100,90,80,70,60,50])
print(array_name.index(90))
print(array_name.index(60))

```
Output
```
1
4
```
